#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../spm-build/bin/spm-build" "$@"
  ret=$?
else 
  node  "$basedir/../spm-build/bin/spm-build" "$@"
  ret=$?
fi
exit $ret
